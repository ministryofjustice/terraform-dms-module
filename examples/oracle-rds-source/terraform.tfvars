# No variables required — VPC and subnets are discovered from the shared MP VPC via data sources.
# KMS key and Secrets Manager secrets are created by this config.
# Run: aws-vault exec data-factory-laa-development -- terraform apply
